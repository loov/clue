---
phase: 10-windows-msvc
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - internal/build/toolchain_msvc_test.go
  - internal/build/response_file_test.go
  - internal/build/msvc_discovery_test.go
autonomous: true

must_haves:
  truths:
    - "MSVCToolchain interface implementation is verified"
    - "Flag generation produces correct MSVC flags for all config options"
    - "Response file generation is tested for threshold and format"
    - "MSVC discovery error messages are helpful"
    - "All tests pass on Linux (using stubs/mocks)"
  artifacts:
    - path: "internal/build/toolchain_msvc_test.go"
      provides: "Tests for MSVCToolchain interface and flag generation"
      contains: "TestMSVCToolchain"
    - path: "internal/build/response_file_test.go"
      provides: "Tests for response file generation"
      contains: "TestResponseFile"
    - path: "internal/build/msvc_discovery_test.go"
      provides: "Tests for MSVC discovery error handling"
      contains: "TestMSVCDiscovery"
  key_links:
    - from: "internal/build/toolchain_msvc_test.go"
      to: "internal/build/toolchain_msvc.go"
      via: "test functions exercise interface methods"
      pattern: "MSVCToolchain"
    - from: "internal/build/response_file_test.go"
      to: "internal/build/response_file.go"
      via: "test functions verify threshold and format"
      pattern: "MaybeUseResponseFile"
---

<objective>
Create comprehensive tests for MSVC toolchain, response files, and discovery.

Purpose: Verify MSVC implementation works correctly before shipping. Tests run on Linux using stubs/mocks since actual VS is only available on Windows.

Output: Test files covering flag generation, response file threshold, and error handling.
</objective>

<execution_context>
@/home/node/.claude/get-shit-done/workflows/execute-plan.md
@/home/node/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-windows-msvc/10-CONTEXT.md
@.planning/phases/10-windows-msvc/10-RESEARCH.md
@.planning/phases/10-windows-msvc/10-01-SUMMARY.md
@.planning/phases/10-windows-msvc/10-02-SUMMARY.md
@.planning/phases/10-windows-msvc/10-03-SUMMARY.md

# Key existing test patterns
@internal/build/toolchain_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MSVC toolchain tests</name>
  <files>internal/build/toolchain_msvc_test.go</files>
  <action>
Create toolchain_msvc_test.go with tests for MSVCToolchain:

1. TestMSVCToolchain_CompilerFlags:
   Test flag generation for different Config values:
   - Default config: should include /nologo, /MT
   - Optimize "fast": should include /O2
   - Optimize "size": should include /O1
   - Optimize "none": should include /Od
   - Warnings "default": should include /W3
   - Warnings "strict": should include /W4
   - Warnings "pedantic": should include /W4, /permissive-
   - WarningsAsErrors true: should include /WX
   - Debug "full": should include /Zi
   - Debug "minimal": should include /Z7
   - Debug "full" should use /MTd (debug CRT)

2. TestMSVCToolchain_LinkerFlags:
   Test linker flag generation:
   - Default config: should include /nologo
   - Debug enabled: should include /DEBUG
   - RawLinker flags: should be appended

3. TestMSVCToolchain_Name:
   - Verify Name() returns "msvc"

4. TestMSVCToolchain_String:
   - Verify String() returns "msvc (native)"

5. TestMSVCToolchain_IsCrossCompiler:
   - Verify returns false (cross-compilation not supported in v0.2.0)

6. Test helper to create MSVCToolchain without actual VS:
   - Create mock MSVCInstallation with Environment containing cl.exe path
   - Use this for flag tests (don't need real compiler)

Note: Tests run on Linux - they test flag generation logic, not actual compilation.
Use table-driven tests matching existing toolchain_test.go patterns.
  </action>
  <verify>
Run: go test ./internal/build/ -run TestMSVCToolchain -v
All MSVC toolchain tests pass.
  </verify>
  <done>MSVCToolchain flag generation verified for all config options.</done>
</task>

<task type="auto">
  <name>Task 2: Create response file tests</name>
  <files>internal/build/response_file_test.go</files>
  <action>
Create response_file_test.go with tests for response file generation:

1. TestEstimateCommandLength:
   - Empty args: length 0
   - Single arg "foo": length 4 (3 + 1 for space)
   - Multiple args: sum of lengths plus separators

2. TestMaybeUseResponseFile_BelowThreshold:
   - Create args totaling less than 8000 chars
   - Verify returns original args unchanged
   - Verify cleanup path is empty

3. TestMaybeUseResponseFile_AboveThreshold:
   - Create args totaling more than 8000 chars
   - Verify returns single arg starting with "@"
   - Verify cleanup path is non-empty
   - Verify response file exists
   - Verify response file contains one arg per line
   - Clean up file after test

4. TestWriteResponseFile:
   - Write args to response file
   - Read file back and verify format
   - Verify one argument per line (no spaces between args)
   - Verify file is in temp directory
   - Clean up after test

5. TestMaybeUseResponseFile_ExactThreshold:
   - Test edge case at exactly 8000 chars
   - Should not generate response file (threshold is exclusive)

6. TestResponseFileFormat:
   - Verify arguments with spaces are handled correctly
   - Verify paths with backslashes (Windows paths) work
   - Verify special characters don't break parsing

Note: Tests verify the logic, not Windows-specific behavior. Response files are a portable concept.
  </action>
  <verify>
Run: go test ./internal/build/ -run TestResponseFile -v
All response file tests pass.
Run: go test ./internal/build/ -run TestEstimate -v
Estimation tests pass.
  </verify>
  <done>Response file threshold and format verified through tests.</done>
</task>

<task type="auto">
  <name>Task 3: Create MSVC discovery tests and run full test suite</name>
  <files>internal/build/msvc_discovery_test.go</files>
  <action>
Create msvc_discovery_test.go with tests for discovery error handling:

1. TestMSVCDiscovery_NotFoundError:
   - On non-Windows (Linux), FindMSVC() should return error
   - Verify error message indicates Windows-only

2. TestMSVCError_Message:
   - Create MSVCError with "not_found" type
   - Verify message includes install link
   - Verify message is user-friendly

3. TestMSVCInstallation_Fields:
   - Create MSVCInstallation struct
   - Verify all fields can be set and read
   - Verify Environment map works correctly

4. TestNewToolchain_MSVC_OnLinux:
   - Call NewToolchain("msvc", HostPlatform())
   - Should return error (MSVC not available on Linux)
   - Verify error message is helpful

After creating tests, run full test suite:

5. Run make test to ensure all existing tests still pass
6. Run make lint to ensure code quality
  </action>
  <verify>
Run: go test ./internal/build/ -run TestMSVC -v
MSVC discovery tests pass.
Run: make test
All tests pass (including existing tests).
Run: make lint
All linters pass.
  </verify>
  <done>Full test suite passes including new MSVC tests, all linters pass.</done>
</task>

</tasks>

<verification>
1. `go test ./internal/build/ -v` - all tests pass
2. `make test` - full test suite passes
3. `make lint` - all linters pass
4. Test coverage includes:
   - CompilerFlags for all optimization levels
   - CompilerFlags for all warning levels
   - CompilerFlags for debug/release CRT
   - LinkerFlags for debug mode
   - Response file threshold at 8000 chars
   - Response file one-per-line format
   - MSVC discovery error on non-Windows
</verification>

<success_criteria>
- All MSVC toolchain tests pass
- All response file tests pass
- All discovery tests pass
- Existing tests unchanged and passing
- make lint passes with no new issues
- Tests run successfully on Linux using stubs
</success_criteria>

<output>
After completion, create `.planning/phases/10-windows-msvc/10-04-SUMMARY.md`
</output>
