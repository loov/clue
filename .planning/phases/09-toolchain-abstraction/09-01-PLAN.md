---
phase: 09-toolchain-abstraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/build/toolchain.go
  - internal/build/toolchain_gcc.go
  - internal/build/toolchain_clang.go
autonomous: true

must_haves:
  truths:
    - "Toolchain interface exists with methods for CC(), CXX(), AR(), Name(), IsCrossCompiler()"
    - "Toolchain interface has CompilerFlags() and LinkerFlags() methods"
    - "Toolchain interface has Identity() method returning CompilerIdentity"
    - "GCCToolchain implements Toolchain interface"
    - "ClangToolchain implements Toolchain interface"
    - "NewToolchain factory returns Toolchain interface based on name"
  artifacts:
    - path: "internal/build/toolchain.go"
      provides: "Toolchain interface definition and NewToolchain factory"
      contains: "type Toolchain interface"
    - path: "internal/build/toolchain_gcc.go"
      provides: "GCCToolchain implementation"
      contains: "type GCCToolchain struct"
    - path: "internal/build/toolchain_clang.go"
      provides: "ClangToolchain implementation"
      contains: "type ClangToolchain struct"
  key_links:
    - from: "internal/build/toolchain.go"
      to: "internal/build/toolchain_gcc.go"
      via: "NewToolchain factory switch case"
      pattern: 'case "gcc":'
    - from: "internal/build/toolchain.go"
      to: "internal/build/toolchain_clang.go"
      via: "NewToolchain factory switch case"
      pattern: 'case "clang":'
---

<objective>
Create the Toolchain interface and GCC/Clang implementations.

Purpose: Extract a compiler-agnostic interface from the existing Toolchain struct to enable GCC, Clang, and (later) MSVC to share build logic. This is the foundation for multi-toolchain support.

Output: toolchain.go with interface + factory, toolchain_gcc.go with GCCToolchain, toolchain_clang.go with ClangToolchain
</objective>

<execution_context>
@/home/node/.claude/get-shit-done/workflows/execute-plan.md
@/home/node/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-toolchain-abstraction/09-CONTEXT.md
@.planning/phases/09-toolchain-abstraction/09-RESEARCH.md

Reference existing code:
@internal/build/toolchain.go (current struct-based implementation)
@internal/build/flags.go (flag logic to move into toolchains)
@internal/build/cache.go (CompilerIdentity struct and GetCompilerIdentity helper)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Toolchain interface and shared helpers</name>
  <files>internal/build/toolchain.go</files>
  <action>
Replace the existing toolchain.go content with:

1. **Toolchain interface definition** with methods:
   - `CC() string` - C compiler path
   - `CXX() string` - C++ compiler path
   - `AR() string` - Archiver path
   - `Name() string` - Toolchain name ("gcc" or "clang")
   - `IsCrossCompiler() bool` - Whether cross-compiling
   - `String() string` - Descriptive string for output
   - `CompilerFlags(config Config) []string` - Generate compiler flags
   - `LinkerFlags(config Config, sysLibs []string) []string` - Generate linker flags
   - `Identity() (CompilerIdentity, error)` - Get compiler identity for cache keys

2. **NewToolchain factory function** signature:
   `func NewToolchain(name string, target Platform) (Toolchain, error)`
   - Get cross-compilation prefix using existing crossPrefix()
   - Check CC/CXX environment variables with fallback
   - Switch on name to create GCCToolchain or ClangToolchain
   - Return error for unknown toolchain names
   - Validation via ValidateToolchain happens separately (preserve existing pattern)

3. **Helper functions** (keep or adapt from existing):
   - `crossPrefix(target Platform) string` - unchanged
   - `gnuTripletPrefix(target Platform) string` - unchanged
   - `ValidateToolchain(tc Toolchain) error` - update to use interface methods
   - `getEnvOr(key, fallback string) string` - helper for env vars with fallback

4. **Shared flag mapping helpers** (extract from flags.go):
   - `optimizationFlag(level string) string` - returns -O0/-Os/-O2/-O3
   - `warningFlags(level string) []string` - returns -Wall/-Wextra/-Wpedantic
   - `debugFlag(level string) string` - returns -g1/-g

Keep the variable mappings (optimizationFlags, warningFlags, debugFlags) as package-level vars for now - they'll be used by both toolchain implementations.

Do NOT include the concrete struct types or their methods - those go in separate files.
  </action>
  <verify>
File compiles: `cd /workspace && go build ./internal/build/`
Interface is defined: `grep "type Toolchain interface" internal/build/toolchain.go`
  </verify>
  <done>
toolchain.go contains Toolchain interface with all required methods and NewToolchain factory that references GCCToolchain/ClangToolchain (will compile once implementations exist)
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement GCCToolchain</name>
  <files>internal/build/toolchain_gcc.go</files>
  <action>
Create internal/build/toolchain_gcc.go with GCCToolchain implementation:

1. **GCCToolchain struct** with private fields:
   ```go
   type GCCToolchain struct {
       cc     string
       cxx    string
       ar     string
       target Platform  // for IsCrossCompiler check
   }
   ```

2. **Path accessor methods**: CC(), CXX(), AR(), Name() returns "gcc"

3. **IsCrossCompiler()**: Check if CC contains GNU triplet prefix (contains "-linux-" or "-darwin-")

4. **String()**: Return descriptive string like "gcc (native)" or "aarch64-linux-gnu-gcc (cross)"

5. **CompilerFlags(config Config) []string**: Move logic from CompilerFlagsWithToolchain for gcc case:
   - Optimization flags via helper
   - Warning flags via helper
   - Werror if WarningsAsErrors
   - Debug flags via helper
   - Sanitizer flags (skip "memory" with warning - GCC doesn't support MemorySanitizer)
   - LTO flag (-flto)
   - PIC flag (-fPIC)
   - Coverage flags: GCC uses `-fprofile-arcs -ftest-coverage`
   - Append RawCompiler flags

6. **LinkerFlags(config Config, sysLibs []string) []string**: Move logic from LinkerFlagsWithToolchain for gcc case:
   - System library flags (-l prefix)
   - Debug flags
   - Sanitizer flags (skip memory)
   - LTO flag
   - Coverage: GCC links automatically via -lgcov (no extra flag needed)
   - Append RawLinker flags

7. **Identity() (CompilerIdentity, error)**: Use GetCompilerIdentity(cc) from cache.go
  </action>
  <verify>
File compiles with interface: `cd /workspace && go build ./internal/build/`
Implements interface: `grep "func (t \*GCCToolchain)" internal/build/toolchain_gcc.go | wc -l` (should be 9 methods)
  </verify>
  <done>
GCCToolchain struct implements all Toolchain interface methods with GCC-specific flag generation
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement ClangToolchain</name>
  <files>internal/build/toolchain_clang.go</files>
  <action>
Create internal/build/toolchain_clang.go with ClangToolchain implementation:

1. **ClangToolchain struct** with private fields (same structure as GCC):
   ```go
   type ClangToolchain struct {
       cc     string
       cxx    string
       ar     string
       target Platform
   }
   ```

2. **Path accessor methods**: CC(), CXX(), AR(), Name() returns "clang"

3. **IsCrossCompiler()**: Same logic as GCC (check for GNU triplet in CC)

4. **String()**: Return descriptive string like "clang (native)" or "aarch64-linux-gnu-clang (cross)"

5. **CompilerFlags(config Config) []string**: Move logic from CompilerFlagsWithToolchain for clang case:
   - Optimization flags via shared helper
   - Warning flags via shared helper
   - Werror if WarningsAsErrors
   - Debug flags via shared helper
   - Sanitizer flags (ALL supported including "memory" - Clang supports MemorySanitizer)
   - LTO flag (-flto)
   - PIC flag (-fPIC)
   - Coverage flags: Clang uses `-fprofile-instr-generate -fcoverage-mapping`
   - Append RawCompiler flags

6. **LinkerFlags(config Config, sysLibs []string) []string**: Move logic from LinkerFlagsWithToolchain for clang case:
   - System library flags (-l prefix)
   - Debug flags
   - Sanitizer flags
   - LTO flag
   - Coverage: Clang requires `-fprofile-instr-generate` at link time
   - Append RawLinker flags

7. **Identity() (CompilerIdentity, error)**: Use GetCompilerIdentity(cc) from cache.go

Note the key differences from GCC:
- Clang supports memory sanitizer (no warning/skip)
- Clang coverage uses different flags (-fprofile-instr-generate vs -fprofile-arcs)
- Clang needs -fprofile-instr-generate at link time for coverage
  </action>
  <verify>
File compiles with interface: `cd /workspace && go build ./internal/build/`
Implements interface: `grep "func (t \*ClangToolchain)" internal/build/toolchain_clang.go | wc -l` (should be 9 methods)
Package builds: `cd /workspace && go build ./internal/build/`
  </verify>
  <done>
ClangToolchain struct implements all Toolchain interface methods with Clang-specific flag generation (different coverage flags, full sanitizer support)
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Package compiles: `cd /workspace && go build ./internal/build/`
2. Interface defined: `grep -q "type Toolchain interface" internal/build/toolchain.go`
3. GCC implementation exists: `grep -q "type GCCToolchain struct" internal/build/toolchain_gcc.go`
4. Clang implementation exists: `grep -q "type ClangToolchain struct" internal/build/toolchain_clang.go`
5. Factory function exists: `grep -q "func NewToolchain" internal/build/toolchain.go`

Note: Tests will fail at this point because consumers still use old *Toolchain type. That's expected - Plan 02 updates consumers.
</verification>

<success_criteria>
- Toolchain interface defined with all 9 methods
- GCCToolchain and ClangToolchain both implement the interface
- NewToolchain factory creates correct implementation based on name
- Flag generation logic moved from flags.go into toolchain implementations
- Package compiles (go build ./internal/build/ succeeds)
- Existing tests may fail due to type changes - this is expected until Plan 02 completes
</success_criteria>

<output>
After completion, create `.planning/phases/09-toolchain-abstraction/09-01-SUMMARY.md`
</output>
