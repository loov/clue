---
phase: 17-testdata-projects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - testdata/json-example/clue.cue
  - testdata/json-example/main.cpp
  - testdata/json-example/sample.json
  - testdata/json-example/vendor/nlohmann/json.hpp
autonomous: true

must_haves:
  truths:
    - "testdata/json-example/ contains a complete C++ project"
    - "Project parses JSON file and outputs parsed values"
    - "nlohmann/json is vendored in vendor/nlohmann/json.hpp"
    - "clue.cue configures includes to find vendored header"
  artifacts:
    - path: "testdata/json-example/clue.cue"
      provides: "Build configuration for JSON example"
      contains: "includes.*vendor"
    - path: "testdata/json-example/main.cpp"
      provides: "C++ source using nlohmann/json"
      contains: "#include.*nlohmann/json.hpp"
    - path: "testdata/json-example/sample.json"
      provides: "Test JSON data for parsing"
      contains: "name"
    - path: "testdata/json-example/vendor/nlohmann/json.hpp"
      provides: "Vendored nlohmann/json library"
      min_lines: 1000
  key_links:
    - from: "testdata/json-example/main.cpp"
      to: "testdata/json-example/vendor/nlohmann/json.hpp"
      via: "#include <nlohmann/json.hpp>"
      pattern: "#include.*<nlohmann/json\\.hpp>"
---

<objective>
Create testdata/json-example project demonstrating nlohmann/json as a vendored header-only dependency.

Purpose: Provides a real-world example of using a popular C++ JSON library with Clue's vendored dependency system. Tests header-only library integration.
Output: Complete testdata project that can be built and run to parse JSON data.
</objective>

<execution_context>
@/home/node/.claude/get-shit-done/workflows/execute-plan.md
@/home/node/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-testdata-projects/17-RESEARCH.md

# Existing testdata patterns
@testdata/deps-project/clue.cue
@testdata/deps-project/main.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JSON example project structure</name>
  <files>
    testdata/json-example/clue.cue
    testdata/json-example/main.cpp
    testdata/json-example/sample.json
  </files>
  <action>
Create testdata/json-example/ directory with:

1. **clue.cue** - Build configuration:
```cue
name: "json-example"

toolchain: {
    compiler: "clang"
    std: "c++20"
}

targets: {
    "json-example": {
        name: "json-example"
        type: "executable"
        sources: ["main.cpp"]
        includes: ["vendor"]
    }
}
```

2. **sample.json** - Test data:
```json
{
    "name": "example-project",
    "version": 1,
    "items": [
        {"key": "first", "value": 100},
        {"key": "second", "value": 200}
    ]
}
```

3. **main.cpp** - C++ source (~50-80 lines):
- Include nlohmann/json via `#include <nlohmann/json.hpp>`
- Open and parse sample.json
- Output specific values for test verification:
  - "Parsed name: example-project"
  - "Version: 1"
  - "Items count: 2"
  - Each item's key and value
- Use `using json = nlohmann::json;` alias
- Handle file open errors with clear message
- Return 0 on success, 1 on error

Output must include these exact strings for integration test validation:
- "Parsed name: example-project"
- "Version: 1"
  </action>
  <verify>
Files exist:
- `ls testdata/json-example/clue.cue`
- `ls testdata/json-example/main.cpp`
- `ls testdata/json-example/sample.json`

Content checks:
- `grep -q 'nlohmann/json.hpp' testdata/json-example/main.cpp`
- `grep -q 'includes.*vendor' testdata/json-example/clue.cue`
  </verify>
  <done>
testdata/json-example/ contains clue.cue, main.cpp, and sample.json with proper structure
  </done>
</task>

<task type="auto">
  <name>Task 2: Vendor nlohmann/json header</name>
  <files>
    testdata/json-example/vendor/nlohmann/json.hpp
  </files>
  <action>
Create the vendored nlohmann/json header:

1. Create directory structure: `testdata/json-example/vendor/nlohmann/`

2. Download nlohmann/json single-header from:
   https://github.com/nlohmann/json/releases/download/v3.11.3/json.hpp

   Use curl: `curl -L -o testdata/json-example/vendor/nlohmann/json.hpp "https://github.com/nlohmann/json/releases/download/v3.11.3/json.hpp"`

3. If network unavailable (CI environment), create a minimal mock header that provides enough API surface for the example to compile. The mock should:
   - Define `nlohmann::json` class with basic parsing and access
   - Support `json::parse(std::istream&)`
   - Support `json["key"]` access returning values convertible to string/int
   - Support iteration via range-for
   - Define a working single-header implementation

Note: The real json.hpp is ~25,000 lines. If mock needed, include a comment explaining it's a test fixture.
  </action>
  <verify>
File exists and has content:
- `ls testdata/json-example/vendor/nlohmann/json.hpp`
- `wc -l testdata/json-example/vendor/nlohmann/json.hpp` (should show substantial line count)
  </verify>
  <done>
vendor/nlohmann/json.hpp exists and provides nlohmann::json API
  </done>
</task>

</tasks>

<verification>
Project structure complete:
```
testdata/json-example/
├── clue.cue
├── main.cpp
├── sample.json
└── vendor/
    └── nlohmann/
        └── json.hpp
```

The project should be buildable with Clue (verified by integration test in Plan 04).
</verification>

<success_criteria>
- testdata/json-example/ directory exists with all files
- clue.cue includes vendor/ in include paths
- main.cpp uses nlohmann/json to parse sample.json
- vendor/nlohmann/json.hpp provides the library
- Output strings are deterministic for test validation
</success_criteria>

<output>
After completion, create `.planning/phases/17-testdata-projects/17-01-SUMMARY.md`
</output>
