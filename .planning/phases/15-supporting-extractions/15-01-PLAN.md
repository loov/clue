---
phase: 15-supporting-extractions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cache/cache.go
  - internal/cache/manager.go
  - internal/cache/deps.go
  - internal/cache/cache_test.go
  - internal/cache/manager_test.go
  - internal/cache/deps_test.go
autonomous: true

must_haves:
  truths:
    - "internal/cache package compiles independently"
    - "Cache hashing functions produce identical results to original"
    - "CacheManager tracks file changes correctly"
    - "ParseDepFile works from internal/cache"
    - "All cache tests pass in new location"
  artifacts:
    - path: "internal/cache/cache.go"
      provides: "CacheKey, ComputeFileHash, ComputeCacheKey, NormalizeFlags"
      exports: ["CacheKey", "ComputeFileHash", "ComputeCacheKey", "NormalizeFlags"]
    - path: "internal/cache/manager.go"
      provides: "Manager type with NeedsRebuild, StoreResult, GetCached"
      exports: ["Manager", "Entry", "RebuildReason", "NewManager"]
    - path: "internal/cache/deps.go"
      provides: "ParseDepFile and DependencyInfo for reading .d files"
      exports: ["ParseDepFile", "DependencyInfo"]
    - path: "internal/cache/cache_test.go"
      provides: "Hash function tests"
    - path: "internal/cache/manager_test.go"
      provides: "CacheManager tests"
    - path: "internal/cache/deps_test.go"
      provides: "ParseDepFile tests"
  key_links:
    - from: "internal/cache/cache.go"
      to: "github.com/zeebo/xxh3"
      via: "xxh3.Hash128 calls"
      pattern: "xxh3\\.Hash128"
    - from: "internal/cache/manager.go"
      to: "internal/toolchain"
      via: "toolchain.CompilerIdentity type"
      pattern: "toolchain\\.CompilerIdentity"
    - from: "internal/cache/manager.go"
      to: "internal/cache/deps.go"
      via: "ParseDepFile calls"
      pattern: "ParseDepFile"
---

<objective>
Extract caching code from internal/build into internal/cache package.

Purpose: Create focused package for content hashing, cache invalidation, and incremental build tracking. Separates caching concerns from build orchestration.

Output: internal/cache package with Manager, hash functions, ParseDepFile, and all tests passing.
</objective>

<execution_context>
@/home/node/.claude/get-shit-done/workflows/execute-plan.md
@/home/node/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-supporting-extractions/15-CONTEXT.md
@.planning/phases/15-supporting-extractions/15-RESEARCH.md

Source files to extract:
@internal/build/cache.go
@internal/build/cache_manager.go
@internal/build/deps.go
@internal/build/cache_test.go
@internal/build/cache_manager_test.go
@internal/build/deps_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create internal/cache package with hash functions and deps</name>
  <files>
    internal/cache/cache.go
    internal/cache/deps.go
  </files>
  <action>
Create internal/cache/cache.go by moving code from internal/build/cache.go:

1. Create directory: internal/cache/
2. Move types and functions:
   - CacheKey struct (keep toolchain.CompilerIdentity reference)
   - CompilerIdentity type alias (keep as `type CompilerIdentity = toolchain.CompilerIdentity`)
   - ComputeFileHash function
   - ComputeCacheKey function
   - NormalizeFlags function
   - GetCompilerIdentity variable

3. Update package declaration: `package cache`
4. Keep all imports (encoding/hex, fmt, os, path/filepath, sort, strings, github.com/zeebo/xxh3, internal/toolchain)

Create internal/cache/deps.go by moving code from internal/build/deps.go:

1. Move types and functions:
   - DependencyInfo struct
   - ParseDepFile function

2. Update package declaration: `package cache`
3. Keep imports (fmt, os, strings)

Do NOT change any function signatures or behavior - this is a pure extraction.
  </action>
  <verify>
Run: `go build ./internal/cache/...`
Should compile without errors.
  </verify>
  <done>internal/cache/cache.go and internal/cache/deps.go exist and compile</done>
</task>

<task type="auto">
  <name>Task 2: Create cache manager and tests</name>
  <files>
    internal/cache/manager.go
    internal/cache/cache_test.go
    internal/cache/manager_test.go
    internal/cache/deps_test.go
  </files>
  <action>
Extract cache manager code to internal/cache/manager.go:

1. Move from internal/build/cache_manager.go:
   - CacheEntry struct (rename to Entry for cleaner API: cache.Entry)
   - RebuildReason type and constants
   - CacheManager struct (rename to Manager: cache.Manager)
   - NewCacheManager function (rename to NewManager)
   - All CacheManager methods (loadManifest, saveManifest, NeedsRebuild, StoreResult, GetCached)
   - atomicWrite helper function
   - stringSlicesEqual helper function
   - normalizeIncludePaths helper function

2. Handle Verbosity:
   - The verbosity field is stored but NEVER used in CacheManager
   - Remove the verbosity field and parameter from NewManager
   - This simplifies the API without changing behavior

3. Handle ParseDepFile:
   - ParseDepFile is now in the same package (internal/cache/deps.go)
   - Just call ParseDepFile directly (no package prefix needed)

4. Move tests:
   - Move cache_test.go to internal/cache/cache_test.go
   - Move cache_manager_test.go to internal/cache/manager_test.go
   - Move deps_test.go to internal/cache/deps_test.go
   - Update package declaration to `package cache`
   - Update imports to use cache package types
   - Rename CacheManager references to Manager
   - Rename CacheEntry references to Entry
   - Update NewCacheManager calls to NewManager (remove verbosity param)

5. Run tests to verify extraction is correct.
  </action>
  <verify>
Run: `go test ./internal/cache/... -v`
All tests should pass.
  </verify>
  <done>Cache manager, deps, and all tests moved, tests pass</done>
</task>

</tasks>

<verification>
Run full test suite to ensure no regressions:
```bash
make lint
make test
```

Verify the cache package is independent:
```bash
go build ./internal/cache/...
go test ./internal/cache/...
```

Verify no circular dependencies:
```bash
# cache package should NOT import internal/build
go list -f '{{.Imports}}' ./internal/cache/...
# Should NOT contain "internal/build"
```

Note: internal/build will have broken imports until Plan 04 updates them.
The build package tests may fail temporarily - this is expected.
</verification>

<success_criteria>
1. internal/cache package exists with cache.go, manager.go, and deps.go
2. All types renamed appropriately (Manager, Entry, NewManager)
3. Verbosity parameter removed (unused)
4. ParseDepFile and DependencyInfo moved to internal/cache (not imported from build)
5. All cache tests pass in new location
6. Package compiles independently with no circular dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/15-supporting-extractions/15-01-SUMMARY.md`
</output>
