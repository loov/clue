---
phase: 15-supporting-extractions
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/watch/watcher.go
  - internal/watch/watcher_test.go
autonomous: true

must_haves:
  truths:
    - "internal/watch package compiles independently"
    - "Watcher monitors file changes correctly"
    - "Debounce logic batches rapid changes"
    - "All watch tests pass in new location"
  artifacts:
    - path: "internal/watch/watcher.go"
      provides: "Watcher type with file monitoring and debouncing"
      exports: ["Watcher", "Config", "NewWatcher", "IsRelevantFile", "DefaultDebounceDuration"]
    - path: "internal/watch/watcher_test.go"
      provides: "Watcher unit tests"
  key_links:
    - from: "internal/watch/watcher.go"
      to: "github.com/fsnotify/fsnotify"
      via: "fsnotify.Watcher embedding"
      pattern: "fsnotify\\.Watcher"
---

<objective>
Extract file watching code from internal/build into internal/watch package.

Purpose: Create focused package for file system monitoring, change notification, and debouncing. Separates watch concerns from build orchestration.

Output: internal/watch package with Watcher, config, and all tests passing.
</objective>

<execution_context>
@/home/node/.claude/get-shit-done/workflows/execute-plan.md
@/home/node/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-supporting-extractions/15-CONTEXT.md
@.planning/phases/15-supporting-extractions/15-RESEARCH.md

Source files to extract:
@internal/build/watcher.go
@internal/build/watcher_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create internal/watch package with watcher</name>
  <files>
    internal/watch/watcher.go
  </files>
  <action>
Create internal/watch directory and extract watcher code:

1. Create directory: internal/watch/

2. Move watcher.go to internal/watch/watcher.go:
   - Update package declaration: `package watch`
   - Rename WatchConfig to Config (cleaner API: watch.Config)
   - Keep all types: Watcher (with renamed Config), DefaultDebounceDuration constant
   - Keep all functions: NewWatcher, Start, WatchCount, Stop, IsRelevantFile
   - Keep all methods: eventLoop, handleEvent, HandleEventPath, fireRebuild
   - Keep isConfigFile helper function
   - Keep all imports (path/filepath, strings, sync, time, github.com/fsnotify/fsnotify)

This is self-contained code with no dependencies on other internal packages (only uses fsnotify).
Do NOT change any function signatures or behavior except the WatchConfig -> Config rename.
  </action>
  <verify>
Run: `go build ./internal/watch/...`
Should compile without errors.
  </verify>
  <done>internal/watch package exists and compiles</done>
</task>

<task type="auto">
  <name>Task 2: Move watch tests</name>
  <files>
    internal/watch/watcher_test.go
  </files>
  <action>
Move test file to internal/watch:

1. Move watcher_test.go to internal/watch/watcher_test.go:
   - Update package declaration: `package watch`
   - Update imports to remove build package references
   - Change build.WatchConfig to Config
   - Change build.NewWatcher to NewWatcher
   - Change build.Watcher to Watcher
   - Change build.DefaultDebounceDuration to DefaultDebounceDuration
   - Change build.IsRelevantFile to IsRelevantFile

2. Run tests to verify extraction is correct.
  </action>
  <verify>
Run: `go test ./internal/watch/... -v`
All tests should pass.
  </verify>
  <done>Watch tests moved and passing</done>
</task>

</tasks>

<verification>
Run full test suite to ensure no regressions:
```bash
make lint
make test
```

Verify the watch package is independent:
```bash
go build ./internal/watch/...
go test ./internal/watch/...
```

Note: internal/build will have broken imports until Plan 04 updates them.
</verification>

<success_criteria>
1. internal/watch package exists with watcher.go
2. WatchConfig renamed to Config for cleaner API
3. No dependencies on internal/build
4. All watch tests pass in new location
5. Package compiles independently
</success_criteria>

<output>
After completion, create `.planning/phases/15-supporting-extractions/15-03-SUMMARY.md`
</output>
