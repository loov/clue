---
phase: 15-supporting-extractions
plan: 04
type: execute
wave: 2
depends_on: ["15-01", "15-02", "15-03"]
files_modified:
  - internal/build/builder.go
  - internal/build/compiler.go
  - internal/build/parallel.go
  - internal/build/cache.go
  - internal/build/cache_manager.go
  - internal/build/profiler.go
  - internal/build/chrome_trace.go
  - internal/build/watcher.go
autonomous: true

must_haves:
  truths:
    - "internal/build compiles successfully"
    - "internal/build imports from cache, profile, watch packages"
    - "All build tests pass"
    - "No duplicate code between build and extracted packages"
    - "make lint passes"
    - "make test passes"
  artifacts:
    - path: "internal/build/builder.go"
      provides: "Builder using cache.Manager and profile.Profiler"
      contains: "cache.NewManager"
    - path: "internal/build/parallel.go"
      provides: "Parallel execution using profile.Profiler"
      contains: "profile.Profiler"
  key_links:
    - from: "internal/build/builder.go"
      to: "internal/cache"
      via: "import and usage"
      pattern: "cache\\.NewManager"
    - from: "internal/build/builder.go"
      to: "internal/profile"
      via: "import and usage"
      pattern: "profile\\.NewProfiler"
    - from: "internal/build/builder.go"
      to: "internal/watch"
      via: "import and usage"
      pattern: "watch\\.NewWatcher"
---

<objective>
Update internal/build to import from extracted packages and remove duplicate code.

Purpose: Complete the extraction by wiring internal/build to use the new cache, profile, and watch packages. Remove original files that have been extracted.

Output: Clean internal/build package that delegates to extracted packages, all tests passing.
</objective>

<execution_context>
@/home/node/.claude/get-shit-done/workflows/execute-plan.md
@/home/node/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-supporting-extractions/15-CONTEXT.md

Prior plan summaries (for types and API changes):
@.planning/phases/15-supporting-extractions/15-01-SUMMARY.md
@.planning/phases/15-supporting-extractions/15-02-SUMMARY.md
@.planning/phases/15-supporting-extractions/15-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update internal/build to use extracted packages</name>
  <files>
    internal/build/builder.go
    internal/build/compiler.go
    internal/build/parallel.go
  </files>
  <action>
Update internal/build files to import from new packages:

1. Update builder.go:
   - Add imports:
     - "github.com/loov/clue/internal/cache"
     - "github.com/loov/clue/internal/profile"
     - "github.com/loov/clue/internal/watch"
   - Change cacheManager type from *CacheManager to *cache.Manager
   - Change profiler type from *Profiler to *profile.Profiler
   - Change NewCacheManager calls to cache.NewManager (note: verbosity param removed)
   - Change NewProfiler calls to profile.NewProfiler
   - Change WatchConfig to watch.Config
   - Change NewWatcher to watch.NewWatcher

2. Update compiler.go:
   - Add import for cache package if needed
   - Update any CacheManager references to cache.Manager
   - Update any CacheKey references to cache.CacheKey
   - Update any RebuildReason references to cache.RebuildReason
   - Update ComputeFileHash calls to cache.ComputeFileHash

3. Update parallel.go:
   - Add import for profile package
   - Update any Profiler references to profile.Profiler
   - Update any CompileEvent references to profile.CompileEvent

4. Update any other files that reference the extracted types:
   - Search for CacheManager, CacheKey, CacheEntry, RebuildReason
   - Search for Profiler, CompileEvent, ChromeEvent, ChromeTrace
   - Search for Watcher, WatchConfig, IsRelevantFile
   - Update all references to use the new package-qualified names

Do NOT create type aliases in internal/build - per CONTEXT.md, callers must import directly from new packages.
  </action>
  <verify>
Run: `go build ./internal/build/...`
Should compile without errors.
  </verify>
  <done>internal/build compiles with new package imports</done>
</task>

<task type="auto">
  <name>Task 2: Remove extracted source files</name>
  <files>
    internal/build/cache.go
    internal/build/cache_manager.go
    internal/build/profiler.go
    internal/build/chrome_trace.go
    internal/build/watcher.go
    internal/build/cache_test.go
    internal/build/cache_manager_test.go
    internal/build/profiler_test.go
    internal/build/chrome_trace_test.go
    internal/build/watcher_test.go
  </files>
  <action>
Remove the original files that have been extracted:

1. Delete source files (now in extracted packages):
   - rm internal/build/cache.go
   - rm internal/build/cache_manager.go
   - rm internal/build/profiler.go
   - rm internal/build/chrome_trace.go
   - rm internal/build/watcher.go

2. Delete test files (now in extracted packages):
   - rm internal/build/cache_test.go
   - rm internal/build/cache_manager_test.go
   - rm internal/build/profiler_test.go
   - rm internal/build/chrome_trace_test.go
   - rm internal/build/watcher_test.go

3. Verify no duplicate code remains.
  </action>
  <verify>
Run: `ls internal/build/*.go | wc -l`
Should be fewer files than before.
  </verify>
  <done>Extracted files removed from internal/build</done>
</task>

<task type="auto">
  <name>Task 3: Run full verification</name>
  <files>
    (none - verification only)
  </files>
  <action>
Run complete verification to ensure extraction is successful:

1. Run linter:
   - make lint
   - Fix any issues found

2. Run all tests:
   - make test
   - All tests must pass

3. Verify package structure:
   - go build ./internal/cache/...
   - go build ./internal/profile/...
   - go build ./internal/watch/...
   - go build ./internal/build/...
   - go build ./...

4. Check for any remaining references to old types:
   - Grep for "build.CacheManager" (should not exist)
   - Grep for "build.Profiler" (should not exist in main code, may exist in tests)
   - Grep for "build.Watcher" (should not exist)
  </action>
  <verify>
Run: `make lint && make test`
Both commands must succeed.
  </verify>
  <done>Full test suite passes, linting passes, extraction complete</done>
</task>

</tasks>

<verification>
Complete verification checklist:
```bash
# All packages compile
go build ./internal/cache/...
go build ./internal/profile/...
go build ./internal/watch/...
go build ./internal/build/...
go build ./...

# All tests pass
make test

# All linters pass
make lint
```

Verify dependency direction:
- internal/build imports internal/cache (correct)
- internal/build imports internal/profile (correct)
- internal/build imports internal/watch (correct)
- None of the extracted packages import internal/build (verify no circular imports)
</verification>

<success_criteria>
1. internal/build compiles and uses new packages
2. No duplicate code between build and extracted packages
3. All extracted source files removed from internal/build
4. All tests pass (make test)
5. All linters pass (make lint)
6. Dependency direction is correct (no circular imports)
</success_criteria>

<output>
After completion, create `.planning/phases/15-supporting-extractions/15-04-SUMMARY.md`
</output>
