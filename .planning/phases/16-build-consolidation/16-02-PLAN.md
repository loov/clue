---
phase: 16-build-consolidation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - main.go
  - internal/generate/ninja.go
  - internal/generate/compdb.go
  - internal/generate/common.go
  - internal/generate/ninja_test.go
  - internal/generate/integration_test.go
autonomous: true

must_haves:
  truths:
    - "main.go imports toolchain package directly for Platform and related functions"
    - "internal/generate imports toolchain package directly for Platform and related types"
    - "Type alias files (platform.go, flags.go, response_file.go) are removed from internal/build"
    - "Build and all tests pass with direct imports"
  artifacts:
    - path: "main.go"
      provides: "CLI entry point using direct toolchain imports"
      contains: "internal/toolchain"
    - path: "internal/generate/ninja.go"
      provides: "Ninja generator using direct toolchain imports"
      contains: "internal/toolchain"
  key_links:
    - from: "main.go"
      to: "internal/toolchain"
      via: "direct import"
      pattern: 'import.*"github.com/loov/clue/internal/toolchain"'
    - from: "internal/generate/ninja.go"
      to: "internal/toolchain"
      via: "direct import for Platform type"
      pattern: 'toolchain\.Platform'
---

<objective>
Remove type alias files from internal/build and update all callers to import directly from source packages.

Purpose: Per CONTEXT.md, this is a clean break - no re-exports. Callers (main.go, internal/generate) import directly from source packages (toolchain). This completes the package extraction by establishing proper dependency flow.

Output: Cleaner internal/build package with only orchestration code; callers using direct imports.
</objective>

<execution_context>
@/home/node/.claude/get-shit-done/workflows/execute-plan.md
@/home/node/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-build-consolidation/16-CONTEXT.md
@.planning/phases/16-build-consolidation/16-RESEARCH.md
@internal/build/platform.go
@internal/build/toolchain.go
@internal/build/flags.go
@internal/build/response_file.go
@main.go
@internal/generate/ninja.go
@internal/generate/compdb.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update main.go to use direct toolchain imports</name>
  <files>main.go</files>
  <action>
Update main.go to import directly from internal/toolchain:

1. Add import: `"github.com/loov/clue/internal/toolchain"`

2. Replace all usages:
   - `build.Platform` -> `toolchain.Platform`
   - `build.HostPlatform()` -> `toolchain.HostPlatform()`
   - `build.ParseTarget(target)` -> `toolchain.ParseTarget(target)`

3. Keep existing build imports for Builder, Options, Result, etc.

Key locations to update:
- Line 234: `var targetPlatform build.Platform`
- Line 236: `targetPlatform = build.HostPlatform()`
- Line 239: `targetPlatform, err = build.ParseTarget(target)`
- Line 248: `targetPlatform == build.HostPlatform()`
- Line 462-466: Similar pattern in runGenerate
- Line 501: `func generateNinja(dir string, cfg *config.Config, platform build.Platform)`
- Line 532: `func generateCompileCommands(dir string, cfg *config.Config, variant string, _ build.Platform)`
- Line 619-626: Similar pattern in runWatch
  </action>
  <verify>`go build ./...` passes</verify>
  <done>main.go imports toolchain directly, no longer uses build.Platform or build.HostPlatform</done>
</task>

<task type="auto">
  <name>Task 2: Update internal/generate to use direct toolchain imports</name>
  <files>internal/generate/ninja.go, internal/generate/compdb.go, internal/generate/common.go, internal/generate/ninja_test.go, internal/generate/integration_test.go</files>
  <action>
Update internal/generate package to import directly from toolchain:

**ninja.go:**
1. Add import: `"github.com/loov/clue/internal/toolchain"`
2. Replace:
   - `build.Platform` -> `toolchain.Platform` (struct type at line 25)
   - `build.NewToolchain` -> Uses toolchain/all.NewToolchain, but keep build.NewToolchain since it's the factory
   - `build.Toolchain` -> `toolchain.Toolchain` (interface type at lines 168, 186)
   - `build.Config` -> `toolchain.Config` (at lines 273, 304, 335)

**compdb.go:**
1. Add import: `"github.com/loov/clue/internal/toolchain"`
2. Replace:
   - `build.HostPlatform()` -> `toolchain.HostPlatform()`
   - `build.NewToolchain` -> keep as-is (factory function)

**common.go:** Check for any build.Platform usage and update

**ninja_test.go:**
1. Add import: `"github.com/loov/clue/internal/toolchain"`
2. Replace all `build.Platform{...}` -> `toolchain.Platform{...}`

**integration_test.go:**
1. Add import: `"github.com/loov/clue/internal/toolchain"` (if needed)
2. Replace `build.HostPlatform()` -> `toolchain.HostPlatform()`
  </action>
  <verify>`go test ./internal/generate/...` passes</verify>
  <done>internal/generate uses toolchain package directly for Platform and Config types</done>
</task>

<task type="auto">
  <name>Task 3: Remove type alias files from internal/build</name>
  <files>internal/build/platform.go, internal/build/flags.go, internal/build/response_file.go</files>
  <action>
Remove the type alias files that are no longer needed:

1. Delete `internal/build/platform.go` (20 lines - Platform type alias and function aliases)
2. Delete `internal/build/flags.go` (7 lines - Config type alias only)
3. Delete `internal/build/response_file.go` (23 lines - ResponseFileThreshold constant and function aliases)

Keep `internal/build/toolchain.go` for now - it contains:
- NewToolchain factory function (delegates to all.NewToolchain)
- TryToolchains function alias
- ValidateToolchain function alias
- FindMSVC function alias
- crossPrefix and gnuTripletPrefix helper functions used by tests
- isCrossCompiler helper function used by tests

These should stay in build package because:
- NewToolchain is the build package's factory for creating toolchains
- The helper functions are used by build package tests

Run `go build ./...` to verify no broken imports.
  </action>
  <verify>
`go build ./...` passes
`go test ./...` passes
`ls internal/build/platform.go internal/build/flags.go internal/build/response_file.go 2>&1 | grep -c "No such file"` returns 3
  </verify>
  <done>Type alias files removed; build package only has orchestration code and necessary toolchain factory</done>
</task>

</tasks>

<verification>
```bash
# Verify alias files are removed
ls internal/build/platform.go internal/build/flags.go internal/build/response_file.go 2>&1

# Verify build passes
go build ./...

# Verify all tests pass
go test ./...

# Verify main.go uses toolchain directly
grep -c "toolchain.Platform\|toolchain.HostPlatform\|toolchain.ParseTarget" main.go

# Verify internal/generate uses toolchain directly
grep -c "toolchain.Platform" internal/generate/ninja.go
```
</verification>

<success_criteria>
- platform.go, flags.go, response_file.go deleted from internal/build
- main.go imports and uses toolchain package directly for Platform operations
- internal/generate imports and uses toolchain package directly for Platform and Config types
- All tests pass (116+)
- No import cycles or broken references
</success_criteria>

<output>
After completion, create `.planning/phases/16-build-consolidation/16-02-SUMMARY.md`
</output>
