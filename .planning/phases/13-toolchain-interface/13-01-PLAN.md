---
phase: 13-toolchain-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/toolchain/toolchain.go
  - internal/toolchain/config.go
  - internal/toolchain/platform.go
  - internal/toolchain/identity.go
  - internal/toolchain/response_file.go
  - internal/toolchain/flags.go
autonomous: true

must_haves:
  truths:
    - "internal/toolchain package exists with Toolchain interface"
    - "Config, Platform, CompilerIdentity types exported from toolchain package"
    - "Response file utilities available in toolchain package"
    - "Flag mapping helpers exported for implementations"
  artifacts:
    - path: "internal/toolchain/toolchain.go"
      provides: "Toolchain interface definition"
      exports: ["Toolchain"]
    - path: "internal/toolchain/config.go"
      provides: "Config type for compiler flags"
      exports: ["Config"]
    - path: "internal/toolchain/platform.go"
      provides: "Platform type and host detection"
      exports: ["Platform", "HostPlatform", "ParseTarget"]
    - path: "internal/toolchain/identity.go"
      provides: "CompilerIdentity type and helper"
      exports: ["CompilerIdentity", "GetCompilerIdentity"]
    - path: "internal/toolchain/response_file.go"
      provides: "Response file utilities"
      exports: ["WriteResponseFile", "MaybeUseResponseFile", "ResponseFileThreshold"]
    - path: "internal/toolchain/flags.go"
      provides: "Flag mapping tables and helpers"
      exports: ["OptimizationFlag", "WarningFlagsForLevel", "DebugFlag"]
  key_links:
    - from: "internal/toolchain/toolchain.go"
      to: "internal/toolchain/config.go"
      via: "method signatures reference Config"
      pattern: "config Config"
    - from: "internal/toolchain/toolchain.go"
      to: "internal/toolchain/identity.go"
      via: "Identity method returns CompilerIdentity"
      pattern: "CompilerIdentity"
---

<objective>
Create the internal/toolchain package with interface definition, shared types, and utilities

Purpose: Establish the foundation package that internal/build will import for toolchain abstraction
Output: internal/toolchain package with Toolchain interface, Config, Platform, CompilerIdentity, response file utilities, and flag helpers
</objective>

<execution_context>
@/home/node/.claude/get-shit-done/workflows/execute-plan.md
@/home/node/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-toolchain-interface/13-CONTEXT.md
@.planning/phases/13-toolchain-interface/13-RESEARCH.md

Source files to extract from:
@internal/build/toolchain.go
@internal/build/flags.go
@internal/build/platform.go
@internal/build/cache.go (CompilerIdentity and GetCompilerIdentity)
@internal/build/response_file.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create toolchain package with interface and core types</name>
  <files>
    internal/toolchain/toolchain.go
    internal/toolchain/config.go
    internal/toolchain/platform.go
    internal/toolchain/identity.go
  </files>
  <action>
Create internal/toolchain directory and four core files:

1. **toolchain.go** - Interface definition only:
   - Package toolchain with doc comment
   - Toolchain interface with 9 methods (CC, CXX, AR, Name, IsCrossCompiler, String, CompilerFlags, LinkerFlags, Identity)
   - Interface uses Config, Platform, CompilerIdentity from same package
   - Add ValidateToolchain function (moved from internal/build/toolchain.go)
   - Add isCrossCompiler helper function (unexported, shared by implementations)
   - Do NOT include NewToolchain factory or implementation structs (those stay in build for now)

2. **config.go** - Config type:
   - Copy Config struct exactly from internal/build/flags.go
   - All fields preserved: Optimize, Warnings, WarningsAsErrors, Debug, RawCompiler, RawLinker, Sanitizers, LTO, PIC, Coverage

3. **platform.go** - Platform type and utilities:
   - Copy Platform struct from internal/build/platform.go
   - Copy HostPlatform, String, IsCrossCompile methods
   - Copy supportedPlatforms map, IsSupportedTarget, SupportedTargetsList, ParseTarget functions
   - Preserve all comments and formatting

4. **identity.go** - CompilerIdentity and GetCompilerIdentity:
   - Copy CompilerIdentity struct from internal/build/cache.go
   - Copy GetCompilerIdentity function from internal/build/cache.go
   - This is the shared utility implementations use for Identity() method
  </action>
  <verify>
go build ./internal/toolchain
go vet ./internal/toolchain
  </verify>
  <done>
internal/toolchain package compiles with exported Toolchain interface, Config, Platform, CompilerIdentity types
  </done>
</task>

<task type="auto">
  <name>Task 2: Add response file utilities and flag helpers</name>
  <files>
    internal/toolchain/response_file.go
    internal/toolchain/flags.go
  </files>
  <action>
Add shared utility files to internal/toolchain:

1. **response_file.go** - Response file utilities:
   - Copy entire file content from internal/build/response_file.go
   - Change package declaration from `build` to `toolchain`
   - Exports: ResponseFileThreshold, WriteResponseFile, MaybeUseResponseFile, EstimateCommandLength, QuoteResponseFileArg

2. **flags.go** - Flag mapping tables and helpers:
   - Copy flag mapping variables from internal/build/toolchain.go (lines 141-164):
     - optimizationFlags map
     - warningFlags map
     - debugFlags map
   - Copy helper functions (lines 166-190):
     - optimizationFlag (rename to OptimizationFlag - export for implementations)
     - warningFlagsForLevel (rename to WarningFlagsForLevel - export for implementations)
     - debugFlag (rename to DebugFlag - export for implementations)
   - Export these so GCC/Clang/MSVC implementations in internal/build can use them
  </action>
  <verify>
go build ./internal/toolchain
go vet ./internal/toolchain
  </verify>
  <done>
Response file utilities and flag helpers available as exported functions in internal/toolchain
  </done>
</task>

<task type="auto">
  <name>Task 3: Create empty subpackage directories for Phase 14</name>
  <files>
    internal/toolchain/gcc/.gitkeep
    internal/toolchain/clang/.gitkeep
    internal/toolchain/msvc/.gitkeep
  </files>
  <action>
Create empty directories that Phase 14 will populate:

1. Create internal/toolchain/gcc/ with .gitkeep file
2. Create internal/toolchain/clang/ with .gitkeep file
3. Create internal/toolchain/msvc/ with .gitkeep file

Each .gitkeep file should contain a comment:
```
# Phase 14: Toolchain implementation will be moved here
```

This prepares the structure for Phase 14 without creating any Go code in subpackages yet.
  </action>
  <verify>
ls -la internal/toolchain/gcc internal/toolchain/clang internal/toolchain/msvc
  </verify>
  <done>
Empty gcc/, clang/, msvc/ directories exist under internal/toolchain for Phase 14
  </done>
</task>

</tasks>

<verification>
Run from project root:

```bash
# Package compiles
go build ./internal/toolchain

# No lint errors
make vet

# Verify exports are accessible
go doc ./internal/toolchain | grep -E "(type|func) [A-Z]"
```

Expected exports:
- type Toolchain interface
- type Config struct
- type Platform struct
- type CompilerIdentity struct
- func ValidateToolchain
- func HostPlatform
- func ParseTarget
- func IsSupportedTarget
- func SupportedTargetsList
- func GetCompilerIdentity
- func WriteResponseFile
- func MaybeUseResponseFile
- func EstimateCommandLength
- func QuoteResponseFileArg
- func OptimizationFlag
- func WarningFlagsForLevel
- func DebugFlag
- const ResponseFileThreshold
</verification>

<success_criteria>
1. internal/toolchain package exists and compiles
2. Toolchain interface has all 9 methods with correct signatures
3. Config, Platform, CompilerIdentity types are exported
4. Response file utilities are exported
5. Flag helpers are exported (OptimizationFlag, WarningFlagsForLevel, DebugFlag)
6. Empty gcc/, clang/, msvc/ subdirectories exist
7. No code duplication with internal/build yet (that's Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/13-toolchain-interface/13-01-SUMMARY.md`
</output>
