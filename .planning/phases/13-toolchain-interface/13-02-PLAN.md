---
phase: 13-toolchain-interface
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - internal/build/toolchain.go
  - internal/build/flags.go
  - internal/build/platform.go
  - internal/build/cache.go
  - internal/build/response_file.go
  - internal/build/toolchain_gcc.go
  - internal/build/toolchain_clang.go
  - internal/build/toolchain_msvc.go
  - internal/build/compiler.go
  - internal/build/linker.go
  - internal/build/builder.go
  - internal/build/parallel.go
  - internal/build/runner.go
  - internal/build/dep_builder.go
autonomous: true

must_haves:
  truths:
    - "internal/build imports internal/toolchain for interface and types"
    - "internal/build implementations use toolchain.Config, toolchain.Platform"
    - "No code duplication between packages"
    - "All existing tests pass without modification"
  artifacts:
    - path: "internal/build/toolchain.go"
      provides: "Type aliases and NewToolchain factory"
      contains: "= toolchain."
    - path: "internal/build/toolchain_gcc.go"
      provides: "GCC implementation using toolchain package types"
      contains: "toolchain.Config"
    - path: "internal/build/toolchain_clang.go"
      provides: "Clang implementation using toolchain package types"
      contains: "toolchain.Config"
    - path: "internal/build/toolchain_msvc.go"
      provides: "MSVC implementation using toolchain package types"
      contains: "toolchain.Config"
  key_links:
    - from: "internal/build/toolchain.go"
      to: "internal/toolchain/toolchain.go"
      via: "import and type alias"
      pattern: 'import.*".*internal/toolchain"'
    - from: "internal/build/toolchain_gcc.go"
      to: "internal/toolchain/config.go"
      via: "method parameter"
      pattern: "toolchain\\.Config"
---

<objective>
Update internal/build to import from internal/toolchain package

Purpose: Establish correct dependency direction (build imports toolchain, not vice versa)
Output: internal/build uses toolchain package types while preserving all behavior
</objective>

<execution_context>
@/home/node/.claude/get-shit-done/workflows/execute-plan.md
@/home/node/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-toolchain-interface/13-01-SUMMARY.md

Files to update:
@internal/build/toolchain.go
@internal/build/flags.go
@internal/build/platform.go
@internal/build/cache.go
@internal/build/response_file.go
@internal/build/toolchain_gcc.go
@internal/build/toolchain_clang.go
@internal/build/toolchain_msvc.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update internal/build to use toolchain package types</name>
  <files>
    internal/build/toolchain.go
    internal/build/flags.go
    internal/build/platform.go
    internal/build/cache.go
    internal/build/response_file.go
  </files>
  <action>
Update internal/build files to import from internal/toolchain and create type aliases:

1. **toolchain.go** - Replace local types with aliases:
   - Add import: `"github.com/example/clue/internal/toolchain"` (use correct module path from go.mod)
   - Replace `type Toolchain interface {...}` with `type Toolchain = toolchain.Toolchain`
   - Remove the interface definition (now in toolchain package)
   - Keep NewToolchain factory function but update to use toolchain.Platform parameter
   - Keep ValidateToolchain or make it call toolchain.ValidateToolchain
   - Remove optimizationFlags, warningFlags, debugFlags maps (moved to toolchain)
   - Remove optimizationFlag, warningFlagsForLevel, debugFlag helpers (now use toolchain.*)
   - Keep isCrossCompiler helper (or use toolchain package version)
   - Keep crossPrefix and gnuTripletPrefix (toolchain construction logic)
   - Keep getEnvOr helper

2. **flags.go** - Replace with type alias:
   - Add import for toolchain package
   - Replace `type Config struct {...}` with `type Config = toolchain.Config`
   - Remove the struct definition (now in toolchain package)

3. **platform.go** - Replace with type aliases:
   - Add import for toolchain package
   - Replace `type Platform struct {...}` with `type Platform = toolchain.Platform`
   - Replace `func HostPlatform()` with `var HostPlatform = toolchain.HostPlatform`
   - Replace other functions with aliases or delegations to toolchain package
   - Remove duplicate definitions

4. **cache.go** - Replace CompilerIdentity with alias:
   - Add import for toolchain package
   - Replace `type CompilerIdentity struct {...}` with `type CompilerIdentity = toolchain.CompilerIdentity`
   - Replace `func GetCompilerIdentity(...)` with `var GetCompilerIdentity = toolchain.GetCompilerIdentity`
   - Keep CacheKey struct and other cache-specific code (not moving to toolchain)
   - Keep ComputeFileHash, ComputeCacheKey, NormalizeFlags (cache concerns, not toolchain)

5. **response_file.go** - Replace with delegations:
   - Add import for toolchain package
   - Replace `const ResponseFileThreshold = 8000` with `const ResponseFileThreshold = toolchain.ResponseFileThreshold`
   - Replace function bodies to call toolchain.* equivalents, OR
   - Delete the file entirely and update callers to use toolchain package directly
   - DECISION: Keep file with aliases for backward compatibility, update in Phase 14
  </action>
  <verify>
go build ./internal/build
go vet ./internal/build
  </verify>
  <done>
internal/build compiles with type aliases pointing to internal/toolchain
  </done>
</task>

<task type="auto">
  <name>Task 2: Update toolchain implementations to use toolchain package</name>
  <files>
    internal/build/toolchain_gcc.go
    internal/build/toolchain_clang.go
    internal/build/toolchain_msvc.go
  </files>
  <action>
Update GCC, Clang, MSVC implementations to use toolchain package types:

1. **toolchain_gcc.go**:
   - Add import for toolchain package
   - Change `func (t *GCCToolchain) CompilerFlags(config Config)` to `func (t *GCCToolchain) CompilerFlags(config toolchain.Config)`
   - OR keep `Config` since it's now an alias in the build package
   - Update calls to flag helpers: `optimizationFlag(...)` -> `toolchain.OptimizationFlag(...)`
   - Update calls to flag helpers: `warningFlagsForLevel(...)` -> `toolchain.WarningFlagsForLevel(...)`
   - Update calls to flag helpers: `debugFlag(...)` -> `toolchain.DebugFlag(...)`
   - Keep implementation logic unchanged

2. **toolchain_clang.go**:
   - Same updates as GCC
   - Flag helper calls updated to use toolchain.* prefix

3. **toolchain_msvc.go**:
   - Add import for toolchain package
   - MSVC has its own flag maps (msvcOptimizationFlags, etc.) - keep these in file
   - Update response file usage if any: use toolchain.MaybeUseResponseFile, etc.
   - Keep MSVC-specific logic unchanged

IMPORTANT: The implementations stay in internal/build for now. Phase 14 moves them to subpackages.
  </action>
  <verify>
go build ./internal/build
go vet ./internal/build
  </verify>
  <done>
GCC, Clang, MSVC implementations compile using toolchain package types and helpers
  </done>
</task>

<task type="auto">
  <name>Task 3: Run all tests to verify no behavior changes</name>
  <files>
    (no files modified - verification only)
  </files>
  <action>
Run the complete test suite to verify refactoring preserves all behavior:

1. Run internal/toolchain tests:
   ```bash
   go test -v ./internal/toolchain
   ```

2. Run internal/build tests:
   ```bash
   go test -v ./internal/build
   ```

3. Run full test suite:
   ```bash
   make test
   ```

4. Run linters:
   ```bash
   make lint
   ```

If any tests fail:
- Check for undefined types (missing import or alias)
- Check for duplicate definitions (type defined in both packages)
- Check for circular imports (toolchain importing build)
- Fix issues before marking task complete

All 55+ files in internal/build should continue to work with the new package structure.
  </action>
  <verify>
make test && make lint
  </verify>
  <done>
All tests pass, all linters pass, no behavior changes from refactoring
  </done>
</task>

</tasks>

<verification>
Run from project root:

```bash
# Full test suite
make test

# All linters
make lint

# Verify no circular dependencies
go build ./...

# Verify toolchain package is imported by build
grep -l 'internal/toolchain' internal/build/*.go
```

Expected:
- All tests pass (same count as before refactoring)
- No lint errors
- Build succeeds
- Multiple build package files import toolchain package
</verification>

<success_criteria>
1. internal/build imports internal/toolchain (not vice versa)
2. Type aliases preserve API compatibility: build.Toolchain = toolchain.Toolchain
3. All implementations (GCC, Clang, MSVC) use toolchain package types
4. Flag helpers called via toolchain.OptimizationFlag, etc.
5. All existing tests pass without modification
6. All linters pass
7. No code duplication - types defined once in toolchain, aliased in build
</success_criteria>

<output>
After completion, create `.planning/phases/13-toolchain-interface/13-02-SUMMARY.md`
</output>
