---
phase: 05-cross-platform-support
plan: 06
type: execute
wave: 3
depends_on: ["05-04", "05-05"]
files_modified:
  - internal/build/integration_cross_platform_test.go
autonomous: true

must_haves:
  truths:
    - "Same CUE config builds on Linux with gcc/clang without modification"
    - "Semantic flags produce correct platform-specific output"
    - "SharedLibraryExtension test verifies .so on Linux, .dylib on macOS target"
    - "Cross-compilation validates toolchain availability before building"
  artifacts:
    - path: "internal/build/integration_cross_platform_test.go"
      provides: "Integration tests for all Phase 5 success criteria"
      min_lines: 150
  key_links:
    - from: "internal/build/integration_cross_platform_test.go"
      to: "internal/build/platform.go"
      via: "Platform detection verification"
      pattern: "HostPlatform|ParseTarget"
    - from: "internal/build/integration_cross_platform_test.go"
      to: "internal/build/builder.go"
      via: "Full build execution"
      pattern: "NewBuilder|Build"
---

<objective>
Create comprehensive integration tests verifying all Phase 5 success criteria.

Purpose: Validate that cross-platform support works end-to-end: same config on different platforms, cross-compilation with correct toolchain, semantic flag mapping, and platform-specific file extensions.

Output: integration_cross_platform_test.go with tests for all 4 success criteria
</objective>

<execution_context>
@/home/node/.claude/get-shit-done/workflows/execute-plan.md
@/home/node/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cross-platform-support/05-RESEARCH.md

Depends on 05-04 (compiler/linker integration), 05-05 (CLI target flag).

Success Criteria to verify:
1. User can build the same project on macOS using Clang without modifying the configuration
2. User can specify cross-compilation target (e.g., linux-arm64) and Clue uses the correct toolchain
3. Semantic flags like "optimization: fast" map to correct platform-specific flags
4. User sees appropriate file extensions for target platform (.so on Linux, .dylib on macOS)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration test file with helper functions</name>
  <files>internal/build/integration_cross_platform_test.go</files>
  <action>
Create integration_cross_platform_test.go with:

1. Package declaration and imports
2. Helper function to create test project with CUE config:
   ```go
   func createCrossPlatformTestProject(t *testing.T, dir string) string
   ```
   - Creates simple C++ hello world project
   - Uses minimal CUE config that works on any platform

3. Helper to check if compiler is available:
   ```go
   func compilerAvailable(compiler string) bool
   ```
   - Uses exec.LookPath to check availability

4. Helper to check if cross-compiler is available:
   ```go
   func crossCompilerAvailable(target Platform) bool
   ```
   - Checks for aarch64-linux-gnu-gcc, etc.
  </action>
  <verify>go build ./internal/build/...</verify>
  <done>Test file created with helper functions</done>
</task>

<task type="auto">
  <name>Task 2: Test Success Criterion 1 - Same config on different platforms</name>
  <files>internal/build/integration_cross_platform_test.go</files>
  <action>
Add TestSameConfigMultiplePlatforms:

1. Create test project with CUE config (no platform-specific settings)
2. Build with native toolchain (clang or gcc, whichever available)
3. Verify build succeeds
4. Verify output artifact exists

This test verifies that the same configuration works on the current platform.
Note: True cross-platform verification requires running on multiple platforms,
but we can test that the config is platform-agnostic.

Add TestPlatformDetection:
1. Verify HostPlatform() returns valid Platform
2. Verify Platform.String() matches expected format
3. Verify IsSupportedTarget() for current platform returns true
  </action>
  <verify>go test -v ./internal/build/... -run TestSameConfig -run TestPlatformDetection</verify>
  <done>Platform-agnostic config test passes</done>
</task>

<task type="auto">
  <name>Task 3: Test Success Criterion 2 - Cross-compilation target</name>
  <files>internal/build/integration_cross_platform_test.go</files>
  <action>
Add TestCrossCompilationTarget:

1. Skip if cross-compiler not available (aarch64-linux-gnu-gcc)
2. Create test project
3. Parse target "linux-arm64" using ParseTarget
4. Discover toolchain for cross-compilation target
5. Verify toolchain CC contains "aarch64-linux-gnu"
6. Verify toolchain AR contains "aarch64-linux-gnu"

Add TestCrossCompilationValidation:
1. Test with unavailable cross-compiler
2. Verify error message is clear
3. Verify error occurs before build starts (upfront validation)

Add TestCrossCompilerNaming:
1. Test crossPrefix for linux-arm64 -> "aarch64-linux-gnu-"
2. Test crossPrefix for linux-amd64 -> "x86_64-linux-gnu-"
  </action>
  <verify>go test -v ./internal/build/... -run TestCrossCompil</verify>
  <done>Cross-compilation target tests pass</done>
</task>

<task type="auto">
  <name>Task 4: Test Success Criterion 3 - Semantic flag mapping</name>
  <files>internal/build/integration_cross_platform_test.go</files>
  <action>
Add TestSemanticFlagMapping:

1. Test optimization: fast -> -O2 (both GCC and Clang)
2. Test warnings: strict -> -Wall -Wextra
3. Test debug: full -> -g
4. Test sanitizers: ["address"] -> -fsanitize=address
5. Test lto: true -> -flto
6. Test pic: true -> -fPIC
7. Test coverage with clang -> -fprofile-instr-generate -fcoverage-mapping
8. Test coverage with gcc -> -fprofile-arcs -ftest-coverage

These are unit-style tests but in integration test file to group all Phase 5 verification.
  </action>
  <verify>go test -v ./internal/build/... -run TestSemanticFlag</verify>
  <done>Semantic flag mapping tests pass</done>
</task>

<task type="auto">
  <name>Task 5: Test Success Criterion 4 - Platform-specific extensions</name>
  <files>internal/build/integration_cross_platform_test.go</files>
  <action>
Add TestPlatformSpecificExtensions:

1. Test SharedLibraryExtension for linux-amd64 -> ".so"
2. Test SharedLibraryExtension for linux-arm64 -> ".so"
3. Test SharedLibraryExtension for darwin-amd64 -> ".dylib"
4. Test SharedLibraryExtension for darwin-arm64 -> ".dylib"

Add TestOutputPathExtensions:
1. Create mock Builder with linux target
2. Verify OutputPath for shared_library includes ".so"
3. Create mock Builder with darwin target
4. Verify OutputPath for shared_library includes ".dylib"
  </action>
  <verify>go test -v ./internal/build/... -run TestPlatformSpecific -run TestOutputPath</verify>
  <done>Platform-specific extension tests pass</done>
</task>

</tasks>

<verification>
1. `go test -v ./internal/build/... -run "Cross|Platform|Semantic|Extension"` all pass
2. Tests verify all 4 Phase 5 success criteria
3. Tests skip gracefully when cross-compilers not installed
4. Test output clearly shows what is being verified
</verification>

<success_criteria>
- TestSameConfigMultiplePlatforms verifies platform-agnostic config
- TestCrossCompilationTarget verifies cross-compiler discovery
- TestSemanticFlagMapping verifies flag translation
- TestPlatformSpecificExtensions verifies .so/.dylib selection
- All tests pass or skip appropriately
- Clear test output describes each criterion
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-platform-support/05-06-SUMMARY.md`
</output>
