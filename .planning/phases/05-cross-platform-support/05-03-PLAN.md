---
phase: 05-cross-platform-support
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/build/flags.go
  - internal/build/flags_test.go
  - internal/config/schema.cue
autonomous: true

must_haves:
  truths:
    - "Sanitizers map to -fsanitize=address|thread|undefined|memory flags"
    - "LTO maps to -flto flag"
    - "PIC maps to -fPIC flag"
    - "Coverage maps to toolchain-specific flags (clang vs gcc)"
    - "MemorySanitizer emits warning on GCC and is skipped"
  artifacts:
    - path: "internal/build/flags.go"
      provides: "Extended semantic flag mapping"
      contains: "Sanitizers|LTO|PIC|Coverage"
    - path: "internal/build/flags_test.go"
      provides: "Extended flag mapping tests"
      min_lines: 80
    - path: "internal/config/schema.cue"
      provides: "Extended CUE schema with new fields"
      contains: "sanitizers|lto|pic|coverage"
  key_links:
    - from: "internal/build/flags.go"
      to: "BuildConfig"
      via: "Extended struct fields"
      pattern: "Sanitizers.*\\[\\]string|LTO.*bool|PIC.*bool|Coverage.*bool"
---

<objective>
Extend semantic flag mapping with sanitizers, LTO, PIC, and coverage instrumentation.

Purpose: Enable modern C++ development workflows with sanitizers for debugging, LTO for optimization, PIC for shared libraries, and coverage for testing. Maps semantic concepts to GCC/Clang-specific flags.

Output: Extended flags.go with sanitizer/LTO/PIC/coverage support, updated schema.cue
</objective>

<execution_context>
@/home/node/.claude/get-shit-done/workflows/execute-plan.md
@/home/node/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cross-platform-support/05-RESEARCH.md
@internal/build/flags.go
@internal/config/schema.cue

Reference Pattern 4 from RESEARCH.md for extended flag mapping.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BuildConfig with new semantic flags</name>
  <files>internal/build/flags.go</files>
  <action>
Extend BuildConfig struct in flags.go:

```go
type BuildConfig struct {
    Optimize         string   // existing
    Warnings         string   // existing
    WarningsAsErrors bool     // existing
    Debug            string   // existing
    RawCompiler      []string // existing
    RawLinker        []string // existing

    // New Phase 5 semantic flags
    Sanitizers []string // "address", "thread", "undefined", "memory"
    LTO        bool     // Link-time optimization
    PIC        bool     // Position-independent code
    Coverage   bool     // Code coverage instrumentation
}
```
  </action>
  <verify>go build ./internal/build/...</verify>
  <done>BuildConfig has Sanitizers, LTO, PIC, Coverage fields</done>
</task>

<task type="auto">
  <name>Task 2: Implement extended flag mapping functions</name>
  <files>internal/build/flags.go</files>
  <action>
Add BuildCompilerFlagsWithToolchain function (or modify existing BuildCompilerFlags):

1. Create BuildCompilerFlagsWithToolchain(config BuildConfig, toolchain string) []string:
   - Include all existing optimization/warnings/debug logic
   - Add sanitizer flags: -fsanitize=address, -fsanitize=thread, etc.
   - Warn and skip "memory" sanitizer on gcc: fmt.Fprintf(os.Stderr, "Warning: MemorySanitizer not available on GCC, skipping\n")
   - Add -flto if config.LTO is true
   - Add -fPIC if config.PIC is true
   - Add coverage flags based on toolchain:
     - clang: -fprofile-instr-generate -fcoverage-mapping
     - gcc: -fprofile-arcs -ftest-coverage

2. Create BuildLinkerFlagsWithToolchain(config BuildConfig, sysLibs []string, toolchain string) []string:
   - Include existing system library and debug logic
   - Add sanitizer flags to linker (must match compiler)
   - Add -flto if config.LTO
   - Add coverage linker flags:
     - clang: -fprofile-instr-generate
     - gcc: (automatic via -lgcov)

Import "os" for stderr warnings.
  </action>
  <verify>go build ./internal/build/...</verify>
  <done>Extended flag functions handle sanitizers/LTO/PIC/coverage</done>
</task>

<task type="auto">
  <name>Task 3: Update CUE schema with new fields</name>
  <files>internal/config/schema.cue</files>
  <action>
Update #Target in schema.cue to add:

```cue
#Target: {
    // ... existing fields ...

    // Extended semantic flags (Phase 5)
    sanitizers?: [...("address" | "thread" | "undefined" | "memory")]
    lto?: bool
    pic?: bool
    coverage?: bool
}
```

Update #Variant to also support these flags for variant-level overrides.
  </action>
  <verify>go test ./internal/config/... -run TestLoad</verify>
  <done>CUE schema accepts sanitizers, lto, pic, coverage fields</done>
</task>

<task type="auto">
  <name>Task 4: Add unit tests for extended flags</name>
  <files>internal/build/flags_test.go</files>
  <action>
Add tests to flags_test.go:

1. TestBuildCompilerFlags_Sanitizers: Verify -fsanitize=address, -fsanitize=thread
2. TestBuildCompilerFlags_MemorySanitizerGCC: Verify warning emitted and flag skipped
3. TestBuildCompilerFlags_MemorySanitizerClang: Verify -fsanitize=memory included
4. TestBuildCompilerFlags_LTO: Verify -flto flag
5. TestBuildCompilerFlags_PIC: Verify -fPIC flag
6. TestBuildCompilerFlags_CoverageClang: Verify -fprofile-instr-generate -fcoverage-mapping
7. TestBuildCompilerFlags_CoverageGCC: Verify -fprofile-arcs -ftest-coverage
8. TestBuildLinkerFlags_Sanitizers: Verify sanitizer flags in linker output
9. TestBuildLinkerFlags_LTO: Verify -flto in linker output
10. TestBuildLinkerFlags_CoverageClang: Verify -fprofile-instr-generate in linker
  </action>
  <verify>go test -v ./internal/build/... -run "Flags|Sanitizer|LTO|PIC|Coverage"</verify>
  <done>All extended flag tests pass</done>
</task>

</tasks>

<verification>
1. `go build ./internal/build/...` succeeds
2. `go test -v ./internal/build/... -run "Flags|Sanitizer|LTO|PIC|Coverage"` all pass
3. `go test ./internal/config/...` passes with updated schema
4. Warning message verified for MemorySanitizer on GCC
</verification>

<success_criteria>
- BuildConfig extended with Sanitizers, LTO, PIC, Coverage
- Sanitizer flags correctly generated for address/thread/undefined/memory
- MemorySanitizer warns and skips on GCC
- LTO adds -flto to both compiler and linker
- PIC adds -fPIC to compiler
- Coverage uses toolchain-specific flags (clang vs gcc)
- CUE schema accepts new fields
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-platform-support/05-03-SUMMARY.md`
</output>
