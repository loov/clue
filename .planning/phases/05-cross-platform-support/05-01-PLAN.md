---
phase: 05-cross-platform-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/build/platform.go
  - internal/build/platform_test.go
autonomous: true

must_haves:
  truths:
    - "HostPlatform() returns current os-arch combination"
    - "ParseTarget() parses 'os-arch' format into Platform struct"
    - "IsSupportedTarget() validates against linux-amd64, linux-arm64, darwin-amd64, darwin-arm64"
    - "Platform.String() returns Go-style 'os-arch' format"
  artifacts:
    - path: "internal/build/platform.go"
      provides: "Platform type and detection functions"
      exports: ["Platform", "HostPlatform", "ParseTarget", "IsSupportedTarget"]
    - path: "internal/build/platform_test.go"
      provides: "Platform detection and parsing tests"
      min_lines: 50
  key_links:
    - from: "internal/build/platform.go"
      to: "runtime.GOOS/GOARCH"
      via: "Go runtime constants"
      pattern: "runtime\\.GOOS|runtime\\.GOARCH"
---

<objective>
Implement platform detection and validation using Go runtime constants.

Purpose: Establish the foundation for cross-platform builds by detecting host platform and validating target platforms. This enables macOS support and cross-compilation.

Output: platform.go with Platform type, HostPlatform(), ParseTarget(), IsSupportedTarget()
</objective>

<execution_context>
@/home/node/.claude/get-shit-done/workflows/execute-plan.md
@/home/node/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cross-platform-support/05-RESEARCH.md

Reference Pattern 1 from RESEARCH.md for Platform type and HostPlatform().
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Platform type and detection</name>
  <files>internal/build/platform.go</files>
  <action>
Create platform.go with:

1. Platform struct with OS and Arch string fields
2. HostPlatform() using runtime.GOOS and runtime.GOARCH
3. Platform.String() returning "os-arch" format
4. Platform.IsCrossCompile() checking if target differs from host
5. supportedPlatforms map with linux-amd64, linux-arm64, darwin-amd64, darwin-arm64
6. IsSupportedTarget(p Platform) bool checking against supportedPlatforms

Import runtime package for GOOS/GOARCH constants.
  </action>
  <verify>go build ./internal/build/...</verify>
  <done>platform.go compiles with Platform type and HostPlatform()</done>
</task>

<task type="auto">
  <name>Task 2: Implement target parsing and validation</name>
  <files>internal/build/platform.go</files>
  <action>
Add to platform.go:

1. ParseTarget(flag string) (Platform, error) function:
   - Split flag on "-" into exactly 2 parts
   - Return error if format invalid: "invalid target format: %s (expected: os-arch)"
   - Create Platform with parts[0] as OS, parts[1] as Arch
   - Validate using IsSupportedTarget
   - Return error if unsupported: "unsupported target: %s\nSupported: linux-amd64, linux-arm64, darwin-amd64, darwin-arm64"

2. SupportedTargetsList() []string for error messages
  </action>
  <verify>go build ./internal/build/...</verify>
  <done>ParseTarget validates format and checks against supported platforms</done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for platform detection</name>
  <files>internal/build/platform_test.go</files>
  <action>
Create platform_test.go with:

1. TestHostPlatform: Verify HostPlatform() returns non-empty OS and Arch
2. TestPlatformString: Verify String() returns "os-arch" format
3. TestParseTarget_Valid: Test parsing linux-amd64, linux-arm64, darwin-amd64, darwin-arm64
4. TestParseTarget_Invalid: Test "invalid", "linux", "linux-arm64-extra", ""
5. TestIsSupportedTarget: Test all 4 supported and several unsupported (windows-amd64, freebsd-amd64)
6. TestPlatformIsCrossCompile: Verify comparison with HostPlatform()
  </action>
  <verify>go test -v ./internal/build/... -run TestPlatform -run TestHostPlatform -run TestParseTarget -run TestIsSupportedTarget</verify>
  <done>All platform tests pass</done>
</task>

</tasks>

<verification>
1. `go build ./internal/build/...` succeeds
2. `go test -v ./internal/build/... -run "Platform|ParseTarget|Supported"` all pass
3. Platform type exports: Platform, HostPlatform, ParseTarget, IsSupportedTarget
</verification>

<success_criteria>
- Platform type with OS/Arch fields exists
- HostPlatform() returns runtime.GOOS and runtime.GOARCH
- ParseTarget() handles valid/invalid input with clear errors
- IsSupportedTarget() validates 4 Phase 5 platforms
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-platform-support/05-01-SUMMARY.md`
</output>
