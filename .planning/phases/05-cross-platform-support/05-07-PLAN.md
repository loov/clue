---
phase: 05-cross-platform-support
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/build/compiler.go
  - internal/build/linker.go
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Compiler passes toolchain name to BuildCompilerFlagsWithToolchain"
    - "Linker passes toolchain name to BuildLinkerFlagsWithToolchain"
    - "Clang coverage uses -fprofile-instr-generate -fcoverage-mapping flags"
    - "GCC memory sanitizer warning appears when memory sanitizer requested"
  artifacts:
    - path: "internal/build/compiler.go"
      provides: "Toolchain-aware compiler flag building"
      contains: "BuildCompilerFlagsWithToolchain(opts.Flags, c.toolchain.Name)"
    - path: "internal/build/linker.go"
      provides: "Toolchain-aware linker flag building"
      contains: "BuildLinkerFlagsWithToolchain(opts.Flags, []string{}, l.toolchain.Name)"
  key_links:
    - from: "internal/build/compiler.go"
      to: "BuildCompilerFlagsWithToolchain"
      via: "direct function call with toolchain.Name"
      pattern: "BuildCompilerFlagsWithToolchain.*c\\.toolchain\\.Name"
    - from: "internal/build/linker.go"
      to: "BuildLinkerFlagsWithToolchain"
      via: "direct function call with toolchain.Name"
      pattern: "BuildLinkerFlagsWithToolchain.*l\\.toolchain\\.Name"
---

<objective>
Wire toolchain-aware flag functions into compiler and linker execution

Purpose: Close verification gap where BuildCompilerFlagsWithToolchain and BuildLinkerFlagsWithToolchain exist but aren't called from compiler.go and linker.go. Currently both files call the wrapper functions that default to "gcc", ignoring the actual toolchain.

Output: Compiler and linker pass their toolchain.Name to flag building functions, enabling toolchain-specific flag variations (Clang coverage, GCC memory sanitizer warnings).
</objective>

<execution_context>
@/home/node/.claude/get-shit-done/workflows/execute-plan.md
@/home/node/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cross-platform-support/05-VERIFICATION.md

@internal/build/compiler.go
@internal/build/linker.go
@internal/build/flags.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire toolchain to compiler flag building</name>
  <files>internal/build/compiler.go</files>
  <action>
    Change line 102 from:
    ```go
    semanticFlags := BuildCompilerFlags(opts.Flags)
    ```
    To:
    ```go
    semanticFlags := BuildCompilerFlagsWithToolchain(opts.Flags, c.toolchain.Name)
    ```

    This single-line change wires the compiler's known toolchain name (available via c.toolchain.Name) into the flag building function, enabling:
    - Clang-specific coverage flags (-fprofile-instr-generate -fcoverage-mapping)
    - GCC memory sanitizer warning (stderr message when msan requested on gcc)
  </action>
  <verify>
    1. File compiles: `go build ./internal/build/...`
    2. Grep confirms wiring: `grep -n "BuildCompilerFlagsWithToolchain.*c\.toolchain\.Name" internal/build/compiler.go`
  </verify>
  <done>compiler.go line 102 calls BuildCompilerFlagsWithToolchain with c.toolchain.Name</done>
</task>

<task type="auto">
  <name>Task 2: Wire toolchain to linker flag building</name>
  <files>internal/build/linker.go</files>
  <action>
    Change line 99 from:
    ```go
    linkerFlags := BuildLinkerFlags(opts.Flags, []string{})
    ```
    To:
    ```go
    linkerFlags := BuildLinkerFlagsWithToolchain(opts.Flags, []string{}, l.toolchain.Name)
    ```

    This single-line change wires the linker's known toolchain name (available via l.toolchain.Name) into the flag building function, enabling:
    - Clang-specific coverage linker flag (-fprofile-instr-generate at link time)
    - Proper sanitizer flag propagation respecting toolchain constraints
  </action>
  <verify>
    1. File compiles: `go build ./internal/build/...`
    2. Grep confirms wiring: `grep -n "BuildLinkerFlagsWithToolchain.*l\.toolchain\.Name" internal/build/linker.go`
  </verify>
  <done>linker.go line 99 calls BuildLinkerFlagsWithToolchain with l.toolchain.Name</done>
</task>

<task type="auto">
  <name>Task 3: Run all tests to verify no regressions</name>
  <files>internal/build/</files>
  <action>
    Run the full test suite to ensure:
    1. Existing flag tests still pass (they call WithToolchain directly)
    2. Compiler tests pass (verify integration doesn't break)
    3. Linker tests pass (verify integration doesn't break)
    4. Integration tests pass (end-to-end verification)

    All tests should continue passing because:
    - Tests that check flag output call WithToolchain functions directly
    - The change only affects how compiler.go and linker.go call the functions
    - Behavior is identical when toolchain.Name matches the default "gcc"
  </action>
  <verify>
    ```bash
    go test ./internal/build/... -v
    ```
    All tests pass.
  </verify>
  <done>All build package tests pass with toolchain wiring in place</done>
</task>

</tasks>

<verification>
Gap closure verified when:
1. `grep -n "BuildCompilerFlagsWithToolchain.*c\.toolchain\.Name" internal/build/compiler.go` finds match
2. `grep -n "BuildLinkerFlagsWithToolchain.*l\.toolchain\.Name" internal/build/linker.go` finds match
3. `go test ./internal/build/... -v` passes all tests
4. `go build ./cmd/clue` succeeds
</verification>

<success_criteria>
- compiler.go calls BuildCompilerFlagsWithToolchain with c.toolchain.Name
- linker.go calls BuildLinkerFlagsWithToolchain with l.toolchain.Name
- All existing tests pass
- Binary compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-platform-support/05-07-SUMMARY.md`
</output>
